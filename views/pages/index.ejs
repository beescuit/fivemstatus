<!DOCTYPE html>
<html>
  <head>
    <title>FiveM Status Page</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    <script src="jquery-3.2.1.min.js"></script>
    <script type="text/javascript">
      setInterval(() => {
        $.get("/json", (data) => {
          var html = "";
          data.forEach((s) => {
            html += `<p>${s[0]}: ${s[1]}</p>`
          })
          $(".statuses").html(html);
        })
      }, 5000)
    </script>
  </head>
  <body>
    <div class="wrapper">
      <div class="app">
        <h1 class="header">FiveM Status Page</h1>
        <div class="statuses">
          <% status.forEach((s) => { %>
            <p><%= s[0] %>: <%- s[1] %></p>
          <% }) %>
        </div>
        <br>
        <p class="footer">This website is not related to the FiveM team.</p>
        <p class="footer">Backend by BeeCoding and Frontend by Amorris</p>
        <a class="footer" style="text-decoration: underline" href="https://github.com/BeeCodingOfc/fivemstatus">Source Code</a>
      </div>
    </div>

    <!-- This should probably be in a separate file, but meh. -->
    <style>
      body {
        height: 100vh;
        margin: 0;
        font-family: 'Roboto', sans-serif;
        background-color: #333;
        color: #bbb;
      }

      .wrapper {
        width: 100%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .app {
        text-align: center;
      }

      .header {
        color: #ddd;
        font-weight: bold;
      }

      .footer {
        color: #666;
      }

    </style>
  </body>
</html>
